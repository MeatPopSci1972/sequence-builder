{"bumpLine":0,"ctx":["0: // SequenceForge dev server v5","1: // node sf-server.js  (use launcher.js for hot-reload)","2: const http     = require('http');","3: const fs       = require('fs');","4: const path     = require('path');","5: const { execFile } = require('child_process');","6: const { SF_ENDPOINTS } = require('./sf-endpoints');","7: const { generateReadme } = require('./sf-readme-gen');","8: const ROOT = path.resolve(__dirname);","9: const PORT = 3799;","10: const MIME = { '.html':'text/html','.js':'text/javascript','.json':'application/json','.md","11: const LOG_BUFFER_DEFAULT = 100;","12: let logBuffer = [];","13: let logHtmlMtime = 0;","14: ","15: function addLog(action, result) {","16:   logBuffer.unshift({ ts: Date.now(), action, result });","17:   if (logBuffer.length > LOG_BUFFER_DEFAULT) logBuffer.length = LOG_BUFFER_DEFAULT;","18: }","19: ","20: // ── CRLF factory ──────────────────────────────────────────────────────────────","21: // normalisePatch(file, old, new) — auto-detects CRLF by reading the first","22: // 512 bytes of the target file on disk. Call sites pass plain LF strings;","23: // no hardcoded file list — the file itself is the source of truth."]}