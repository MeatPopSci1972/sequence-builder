# SequenceForge -- Session Handoff
Version: v0.9.29
Date: 2026-03-19
Repo: https://github.com/MeatPopSci1972/sequence-builder
Local: E:\uml2prompt\sequence-builder-prototype

---

## Step 0 -- Start of session checklist

  cd E:\uml2prompt\sequence-builder-prototype
  node sf-server.js        # window 1 -- leave running all session

Open http://localhost:3799/test in Chrome.
Green badge = 85 passed, safe to proceed.

PowerShell uses semicolons not &&.
Gate = node build.js then node sequence-builder.test.js (or just /test).

---

## Release checklist (every version bump)

This is the proto2prod release flow. Every step happens in the same commit.
No separate snapshot step. No extra decisions.

  1. Gate green: http://localhost:3799/test
  2. Bump version in sequence-builder.html:
       - Header comment:  Version: X.Y.Z
       - Debug console:   SequenceForge vX.Y.Z -- debug console ready
  3. Create snapshot (I do this via the server):
       POST http://localhost:3799/snapshot?v=X.Y.Z
       Copies current build to releases/vX.Y.Z/sequence-builder.html
  4. git add -A
  5. git commit -m "vX.Y.Z - plain ASCII, no special chars"
  6. git push origin main

Latest link always tracks main.
Stable link is pinned: releases/vX.Y.Z/sequence-builder.html

---

## Dev server (sf-server.js)

Start: node sf-server.js
Serves all project files at http://localhost:3799

  Route                  Method  Purpose
  /                      GET     List all files
  /test                  GET     Run build.js + tests, HTML report
  /snapshot?v=X.Y.Z      POST    Copy build to releases/vX.Y.Z/
  /<file>                GET     Read file
  /<file>                PUT     Write file
  /<file>                DELETE  Delete file (temp files)

Temp inspection files: _*.txt prefix, gitignored, auto-deleted by I after each session.

---

## The four files

  sequence-builder.html      ~4400 lines -- deployable
                                           DO NOT edit @@STORE-START...@@STORE-END
                                           Generated by build.js from store.js
  sequence-builder.store.js  ~525 lines  -- CANONICAL store. Edit here, build.js syncs.
  sequence-builder.test.js               -- 85 tests, 10 suites. All must stay green.
  build.js                   ~174 lines  -- syncs store.js into HTML. Zero deps.

---

## Where we left off (v0.9.29)

### What shipped this session

v0.9.28 -- ADD_MESSAGE null contract
  - ADD_MESSAGE now stores fromId: null, toId: null (not undefined) when no actors
  - Suite 10 added: 4 tests covering null contract and UPDATE_MESSAGE wiring
  - 85 tests total

v0.9.29 -- Version strings, snapshot build, release infrastructure
  - Version strings corrected in HTML (header + debug console both said 0.9.9/0.9.26)
  - releases/v0.9.28/sequence-builder.html -- first published stable snapshot
  - README updated: dual links (latest + stable snapshot)
  - sf-server.js: POST /snapshot?v=X.Y.Z route -- creates snapshot in one call
  - sf-server.js: mkdirSync recursive on PUT -- no manual mkdir needed
  - Release checklist documented -- snapshot is now part of every commit, not a separate step

---

## Architecture

  User event
      |
      v
  store.dispatch(action)          <- ONLY mutation entry point
      |
      +-- emit(event, payload)
                |
                v
          store.on() listeners
                |
                +-- setSelected(obj, type, doRender)  <- ONLY selection entry point (v0.9.27)
                +-- render() or requestRender()
                        |
                        v
                    renderElement(el, type, layer, ...)  <- ONLY scaffolding (v0.9.27)
                        |
                        +-- drawFn(g)      type-specific content
                        +-- clickFn(e)     type-specific click

Invariant: store never reads uiState. store never calls render().

---

## State model

  // store.state -- serializable, persistable
  {
    actors:    [{ id, x, label, type, emoji? }],
    messages:  [{ id, fromId, toId, label, kind, direction, y,
                  protocol?, port?, auth?, dataClass? }],
                  fromId and toId are always null or a valid actor id, never undefined
    notes:     [{ id, x, y, text }],
    fragments: [{ id, x, y, w, h, kind, cond }],
    nextId:    Number
  }
  // uiState -- never persisted
  {
    selected:    null | { ...obj, _type, _ref, _preview? },
    mode:        'select' | 'connect' | 'place-actor',
    connectFrom: null | actorId,
    format:      'plantuml' | 'mermaid',
    placeGhostX: null | Number
  }

---

## Six things most likely to confuse a fresh AI instance

1. Never edit @@STORE-START...@@STORE-END in HTML -- generated by build.js
2. _wrapSelected freezes a COPY: Object.freeze({ ...obj }) not obj itself
3. render() is immediate, requestRender() is rAF-batched -- never swap them
4. state = _store.state is a live alias -- writing directly bypasses undo
5. REFLOW_ACTORS for batch moves -- one undoable step, not N dispatches
6. _parseUML returns { actors, messages, warnings } -- warnings always present

---

## Priority queue for next session

  Item                          Priority  Notes
  renderMessage refactor        Med       137 lines, longest render fn. Self-loop path
                                          and network badge logic can be cleaner now
                                          that renderElement owns scaffolding.
  Double [save] in verbose log  Low       render() firing twice in some paths.
                                          Investigate once features stable.
  Touch/mobile drag             Low       Mouse events only currently.

---

## Test suite summary (85 tests, 10 suites)

  Suite  Coverage                                           Count
  1      ADD_ACTOR, REFLOW_ACTORS                           9
  2      DELETE_ACTOR cascade                               4
  3      UPDATE_MESSAGE, pure helpers                       4
  4      meta.undoable, log, timestamps                     4
  5      UNDO                                               6
  6      REDO, branching history                            7
  7      _parseUML                                          9
  8      E2E lifecycle                                      17
  9      No-guard policy, edit button, selection            21
  10     ADD_MESSAGE null contract, UPDATE_MESSAGE wiring   4

Run: node sequence-builder.test.js
Expected: 85 passed | 0 failed | 85 total

---

## Commit message convention

Plain ASCII only. No em-dashes, no Unicode box chars. They corrupt in terminals.
Good:  v0.9.29 - version strings corrected, snapshot build, release infrastructure
Bad:   v0.9.29 -- Template Method (em-dash corrupts on copy-paste)
