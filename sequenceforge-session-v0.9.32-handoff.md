# SequenceForge -- Session Handoff
Version: v0.9.32
Date: 2026-03-19
Repo: https://github.com/MeatPopSci1972/sequence-builder
Local: E:\uml2prompt\sequence-builder-prototype

---

## Step 0 -- Start of session checklist

```
cd E:\uml2prompt\sequence-builder-prototype
node sf-server.js  # window 1 -- leave running all session
```

Open http://localhost:3799/test in Chrome.
Green badge = 85 passed, safe to proceed.
PowerShell uses semicolons not &&.
Gate = node build.js then node sequence-builder.test.js (or just /test).

---

## Release checklist (every version bump)

Every step happens in the same commit. No separate steps. No extra decisions.

1. Gate green: http://localhost:3799/test
2. Tell Claude the version number
3. Claude does all of the following:
   a. Bump version in sequence-builder.html:
      - Header comment: Version: X.Y.Z
      - Debug console: SequenceForge vX.Y.Z -- debug console ready
   b. Review tour steps -- update any step that references changed UI
      Tour steps live in sequence-builder.html inside the STEPS array in the sf-tour-script block.
      Each step has: target, title, body, position.
      Update body text if a feature changed.
      Add a step if a major feature shipped.
      Remove a step if removed.
   c. Call POST http://localhost:3799/snapshot?v=X.Y.Z
      Creates releases/vX.Y.Z/sequence-builder.html automatically
   d. git add -A
   e. git commit -m "vX.Y.Z - plain ASCII description"
   f. git push origin main
   Latest link always tracks main. Stable link is pinned: releases/vX.Y.Z/sequence-builder.html

---

## Tour system

The tour is a self-contained overlay in sequence-builder.html.
It has no external dependencies and no build step of its own.

Location in HTML:
  CSS:    <style id="sf-tour-css"> (in <head>)
  HTML:   (before </body>)
  Script: <script id="sf-tour-script"> (before </body>)

Tour steps array (inside sf-tour-script):
  var STEPS = [
    { target, title, body, position },
    ...
  ]
  target:   CSS selector or element id (e.g. '#btn-add-actor', '#sidebar')
  title:    Short name shown in card header
  body:     HTML string -- use <b> for accent, <kbd> for key labels
  position: 'right' | 'left' | 'bottom' | 'top' | 'center'
            center = full-screen dim, no spotlight, card centered

User preference:
  localStorage key: sf-tour-done
  If set:   tour does not auto-launch on startup
  If cleared: tour launches on next page load

Settings modal:
  "Quick Tour" checkbox syncs to this key
  "Launch tour now" button opens tour and closes settings

Re-launch: click the ? button in the toolbar, or via Settings modal

When to update the tour (at release time):
  New element type added    -> add a step
  Button renamed or moved   -> update that step's target + body
  Feature removed           -> remove its step
  Major workflow changed    -> rewrite affected step body
Keep steps short -- 1-2 sentences each. Users are explorers, not readers.

---

## Dev server (sf-server.js)

| Route               | Method | Purpose                          |
|---------------------|--------|----------------------------------|
| /                   | GET    | List all files                   |
| /test               | GET    | Run build.js + tests, HTML report|
| /snapshot?v=X.Y.Z   | POST   | Copy build to releases/vX.Y.Z/   |
| /<file>             | GET    | Read file                        |
| /<file>             | PUT    | Write file                       |
| /<file>             | DELETE | Delete file (temp files)         |

Temp inspection files: _*.txt prefix, gitignored, cleaned by Claude after each session.

---

## The four files

| File                        | Size       | Notes                                                        |
|-----------------------------|------------|--------------------------------------------------------------|
| sequence-builder.html       | ~4650 lines| deployable. DO NOT edit @@STORE-START...@@STORE-END (generated by build.js) |
| sequence-builder.store.js   | ~525 lines | CANONICAL store. Edit here, build.js syncs.                  |
| sequence-builder.test.js    | 85 tests, 10 suites. All must stay green.                    |
| build.js                    | ~174 lines | syncs store.js into HTML. Zero deps.                         |

---

## Where we left off (v0.9.32)

### What shipped this session

**v0.9.32 -- renderMessage refactor**

renderMessage was 137 lines handling three distinct concerns inline.
Extracted into three focused helpers, renderMessage is now ~55 lines and reads top-to-bottom.

New helpers (live just above renderMessage in the HTML):

  _renderMsgUnconnected(m)
    Dashed placeholder row for messages with no connected actors.
    Clickable, selectable, shows warning label.

  _buildSelfPath(x1, y, cls, kindKey)
    Returns the SVG path element for a self-loop (cubic bezier arc back to same actor).
    Pure: no side effects, no DOM append.

  _appendNetBadges(g, m, lx, ly, dir)
    Appends network protocol/port/auth badge and dataClass badge below the message line.
    classColors map lives here now (was inline in renderMessage).

renderMessage itself is now the coordinator:
  guard -> geometry -> scaffold -> path/line -> label -> chevron -> hover bounds -> click -> badges -> append

Zero behavior change. Gate held at 85/85 throughout.

### Prior versions (for context)

v0.9.31 -- fix palette icons in collapsed (icons) mode adding to canvas
v0.9.30 -- first-run tour, 8-step guided tour with spotlight and dots
v0.9.29 -- version strings fixed, snapshot infrastructure, POST /snapshot route
v0.9.28 -- ADD_MESSAGE null contract (fromId/toId always null not undefined)

---

## Architecture

```
User event
  |
  v
store.dispatch(action)   <- ONLY mutation entry point
  |
  +-- emit(event, payload)
  v
store.on() listeners
  +-- setSelected(obj, type, doRender)  <- ONLY selection entry point
  +-- render() or requestRender()
        |
        v
  renderElement(el, type, layer, ...)  <- ONLY scaffolding
        +-- drawFn(g)   type-specific content
        +-- clickFn(e)  type-specific click
```

Invariant: store never reads uiState. store never calls render().

---

## State model

```
store.state -- serializable, persistable
{
  actors:    [{ id, x, label, type, emoji? }],
  messages:  [{ id, fromId, toId, label, kind, direction, y, protocol?, port?, auth?, dataClass? }],
             fromId/toId always null or valid actor id, never undefined
  notes:     [{ id, x, y, text }],
  fragments: [{ id, x, y, w, h, kind, cond }],
  nextId:    Number
}

uiState -- never persisted
{
  selected:     null | { ...obj, _type, _ref, _preview? },
  mode:         'select' | 'connect' | 'place-actor',
  connectFrom:  null | actorId,
  format:       'plantuml' | 'mermaid',
  placeGhostX:  null | Number
}
```

---

## Six things most likely to confuse a fresh AI instance

1. Never edit @@STORE-START...@@STORE-END in HTML -- generated by build.js
2. _wrapSelected freezes a COPY: Object.freeze({ ...obj }) not obj itself
3. render() is immediate, requestRender() is rAF-batched -- never swap them
4. state = _store.state is a live alias -- writing directly bypasses undo
5. REFLOW_ACTORS for batch moves -- one undoable step, not N
6. _parseUML returns { actors, messages, warnings } -- warnings always present

---

## renderMessage function map (as of v0.9.32)

| Function               | Lines (approx) | Role                                      |
|------------------------|----------------|-------------------------------------------|
| _renderMsgUnconnected  | 20             | Placeholder for unwired messages          |
| _buildSelfPath         | 8              | Self-loop bezier path element             |
| _appendNetBadges       | 25             | Network + dataClass badges below line     |
| renderMessage          | 55             | Coordinator -- routes to above helpers    |

All four live contiguously in sequence-builder.html just above renderNote.

---

## Priority queue for next session

| Priority | Item                                                        |
|----------|-------------------------------------------------------------|
| Low      | Double [save] verbose log                                   |
| Low      | render() firing twice in some paths                         |
| Low      | Touch/mobile drag -- mouse events only                      |
| Low      | Mobile/iPhone layout -- explore feasibility (backlog only)  |
| Med      | Canary test suite -- browser automation smoke tests (sequence-builder.canary.js) |

---

## Future architecture note (no action needed)

When the single-file paradigm is eventually broken for human readability,
the natural split points are:
  sf-store.js   -- already exists, already canonical
  sf-render.js  -- renderActor, renderMessage (+helpers), renderNote, renderFragment
  sf-tour.js    -- already self-contained, lowest-risk first extraction
  sf-ui.js      -- event wiring, toolbar, panels
  sequence-builder.html -- shell + SVG defs + layout only
build.js would evolve from "sync store into HTML" to "assemble deployable from parts."
Same zero-dep philosophy, different scope. Break it when a pain point forces the hand.

---

## Test suite (85 tests, 10 suites)

| Suite | Name                                    | Tests |
|-------|-----------------------------------------|-------|
| 1     | ADD_ACTOR, REFLOW_ACTORS                | 9     |
| 2     | DELETE_ACTOR cascade                    | 4     |
| 3     | UPDATE_MESSAGE, pure helpers            | 4     |
| 4     | meta.undoable, log, timestamps          | 4     |
| 5     | UNDO                                    | 6     |
| 6     | REDO, branching history                 | 7     |
| 7     | _parseUML                               | 9     |
| 8     | E2E lifecycle                           | 17    |
| 9     | No-guard policy, edit button, selection | 21    |
| 10    | ADD_MESSAGE null contract, wiring       | 4     |

---

## Commit message convention

Plain ASCII only. No em-dashes, no Unicode. They corrupt on terminal copy-paste.
Good: `v0.9.32 - refactor renderMessage into three focused helpers`
Bad:  `v0.9.32 -- refactor` (em-dash corrupts)

---

## proto2prod rules (carry forward)

- Validates before optimizing (breadth-first then depth)
- Single-file/zero-dep prototypes to learn before adding infrastructure
- Documents for two audiences: human + fresh AI instance
- Edits surgically -- verifies targets, checks side-effects
- Code should never bury run-time knowledge: visible errors over graceful degradation
- Memory entries refactor over time -- transferable patterns over project-specific detail

UI rule: any element can be added at any time -- no precondition guards
Association between elements done via Properties or drag, never via implicit arm-and-fire mechanics.

SequenceForge release flow (v0.9.29+):
1. Gate green at /test
2. User tells Claude the version number
3. Claude bumps version strings, calls POST /snapshot?v=X.Y.Z, includes everything in one commit
